// name: recursive factorial
// stdout:
//   120

fun fact(n: Int) -> Int {
    if (n == 0) {
        1
    } else {
        n * fact(n - 1)
    }
}

fun main() !io -> Unit {
    print(intToString(fact(5)) + "\n");
}
