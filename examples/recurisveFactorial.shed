// stdout:
//   120

fun fact(n: Int) -> Int {
    if (n == 0) {
        return 1;
    } else {
        return n * fact(n - 1);
    }
}

fun main() !Io -> Unit {
    print(intToString(fact(5)) + "\n");
}
