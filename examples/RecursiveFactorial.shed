// stdout:
//   120

export main;

fun fact(n: Int) -> Int {
    if (n == 0) {
        1
    } else {
        n * fact(n - 1)
    }
}

fun main() !Io -> Unit {
    print!(intToString(fact(5)) + "\n");
}
